# HOW TO RUN THIS PROJECT

## ⚡ Quick Start

### 1️⃣ Install Dependencies
```bash
npm install --legacy-peer-deps
```
⚠️ **IMPORTANT**: Use `--legacy-peer-deps` flag to avoid React 19 compatibility issues!

### 2️⃣ Run the Project

**Option 1: Run with Mock Server (Recommended)**
```bash
npm run dev:full
```

**Option 2: Run Vite Only**
```bash
npm run dev
```

**Option 3: Run Mock Server Separately**
```bash
npm run mock-server
```

### 3️⃣ Open in Browser
```
http://localhost:8080
```

## 📁 Project Structure

This is a **Vite + React** project (not Create React App!)

- ✅ Vite is configured in `vite.config.ts`
- ✅ Entry point is `src/main.tsx`
- ✅ Mock API server at `mock-server/db.json`

## 🎯 What Was Fixed

### 1. Package.json Scripts
**Before:**
```json
"dev": "concurrently \"npm run mock-server\" \"npm start\"",
"start": "react-scripts start"
```

**After:**
```json
"dev": "vite",
"dev:full": "concurrently \"npm run mock-server\" \"vite\""
```

### 2. Vite Configuration
- Added API proxy for mock server
- Fixed path aliases
- Changed host from "::" to "0.0.0.0" for Windows compatibility

### 3. Dependencies
- Installed with `--legacy-peer-deps` to resolve React 19 conflicts

## 🚨 Common Issues & Solutions

### Issue 1: "Cannot find module"
**Solution:**
```bash
rm -rf node_modules package-lock.json
npm install --legacy-peer-deps
```

### Issue 2: Port already in use
**Solution:** Edit `vite.config.ts` and change port 8080 to something else

### Issue 3: Import errors
**Solution:** The project uses both TypeScript and JavaScript - this is normal!

### Issue 4: Mock server not working
**Solution:** Run it separately:
```bash
npm run mock-server
```

## 📝 Available Commands

| Command | Description |
|---------|-------------|
| `npm run dev` | Start Vite dev server (port 8080) |
| `npm start` | Same as dev |
| `npm run dev:full` | Start Vite + Mock API |
| `npm run mock-server` | Run mock API (port 3001) |
| `npm run build` | Build for production |
| `npm run preview` | Preview production build |
| `npm run lint` | Check code quality |

## 🌐 URLs

- **Frontend:** http://localhost:8080
- **Mock API:** http://localhost:3001/api

## 📚 Testing Without Login

Demo routes (auto-login enabled in dev mode):
- http://localhost:8080/parents-demo
- http://localhost:8080/parents-schools-demo  
- http://localhost:8080/parents-evaluations-demo

## 💡 Tech Stack

- **React 19** - UI Framework
- **Vite** - Build tool (fast!)
- **React Router** - Navigation
- **TanStack Query** - Data fetching
- **Tailwind CSS** - Styling
- **Framer Motion** - Animations
- **React Hot Toast** - Notifications

## ✅ Success Checklist

- [ ] Dependencies installed with `--legacy-peer-deps`
- [ ] Running `npm run dev` or `npm run dev:full`
- [ ] Opened http://localhost:8080 in browser
- [ ] No console errors (check browser console)

## 🆘 Still Having Issues?

1. Check the terminal output for specific errors
2. Check the browser console (F12)
3. Make sure you're in the project directory
4. Verify Node.js version: `node --version` (should be 16+)
5. Try clearing cache: `npm run dev -- --force`

---

**Project generated by Lovable.ai and configured for local development**

